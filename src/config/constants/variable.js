const LOCALSTORAGE_KEY = {//本地存储变量的key
  userInfo: 'useInfo_upms',
  currentStaff: 'currentStaff_upms',
  siteType: 'ly_site',
};

const systemConfig = {
  siteType: 'upms',
};

export default {
  LOCALSTORAGE_KEY,
  tokenCookieName: 'intebox_sso_tkt',
  systemConfig,
}
